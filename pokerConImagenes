from tkinter import *
def voltearCarta():
    global cont1

    print(cont1)
    if cont1 % 2 != 0:
        boton1.configure(image=carta3C)
    else:
        boton1.configure(image=cartaR)
    cont1 += 1
def voltearCarta2():
    global cont2
    print(cont2)
    if cont2 %2 !=0:
        boton2.configure(image=cartaASD)
    else:
        boton2.configure(image=cartaR)
    cont2+=1    
def voltearCarta3():
    global cont3
    print(cont3)
    if cont3 %2 !=0:
        boton3.configure(image=cartaJoker)
    else:
        boton3.configure(image=cartaR)
    cont3+=1        
def voltearCarta4():
    global cont4
    print(cont4)
    if cont4 %2 !=0:
        boton4.configure(image=cartaJoker)
    else:
        boton4.configure(image=cartaR)
    cont4+=1
def voltearCarta5():
    global cont5
    print(cont5)
    if cont5 %2 !=0:
        boton5.configure(image=cartaJoker)
    else:
        boton5.configure(image=cartaR)
    cont5+=1          
cont1=1
cont2=1
cont3=1
cont4=1
cont5=1
ventana = Tk()
label1 = Label (ventana, text='Carta1', font= ("Arial bold", 10))
label1.grid(column = 1, row=0)
label1 = Label (ventana, text='Carta1R', font= ("Arial bold", 10))
label1.grid(column = 2, row=0)
Label1=Label(ventana,text="carta2", font=("Arial bold",10))
label1.grid(column=3,row=0)

carta3C = PhotoImage(file = r"carta3C.png")
cartaR = PhotoImage(file = r"cartareves.png")
cartaASD=PhotoImage(file=r"ASD.png")
cartaJoker=PhotoImage(file=r"cartaJoker.png")
